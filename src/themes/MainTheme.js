import { createTheme } from "@mui/material"


export const mainTheme = createTheme({
    palette: {
        primary: {
            main: "#738678"
        },
        secondary: {
            main: "#B5E2FA"
        },
      },
})